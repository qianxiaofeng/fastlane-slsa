source "https://rubygems.org"

# fastlane：iOS 构建、签名(match)、上传(pilot/deliver) 的核心工具链
gem "fastlane"

# CocoaPods 暂未使用；如果示例 app 引入第三方依赖再放开
# gem "cocoapods"

# 未使用 fastlane 插件，故无 Pluginfile 引入。若将来 `fastlane add_plugin` 自动补回
# eval_gemfile 样板，参数必须是字符串字面量——变量会让 Dependabot 解析失败，
# 无法自动创建安全更新 PR（2026-07 已踩坑）。
